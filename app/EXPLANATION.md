## Added this dependencies to the app the package.json file

1.  framer motion - (for animating components)
2.  lucide-react - (for Icons)
3.  react-parallax-tilt - (for making components tilt when hovered on or clicked)

## created all this files:

[any file that starts with `WaitList` was created by me]
